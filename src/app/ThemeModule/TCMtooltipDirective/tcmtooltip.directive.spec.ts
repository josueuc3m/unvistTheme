import { TCMtooltipDirective } from './tcmtooltip.directive';

describe('TCMtooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new TCMtooltipDirective();
    expect(directive).toBeTruthy();
  });
});
